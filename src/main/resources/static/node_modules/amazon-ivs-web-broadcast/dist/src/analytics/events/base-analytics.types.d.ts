/*! Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved. SPDX-License-Identifier: Apache-2.0 */
import { BroadcastSessionEvents } from '../events';
export declare type EventNames = Exclude<ValuesOf<typeof BroadcastSessionEvents>, 'buffer_duration'>;
export interface SpadeEvent {
    event: EventNames;
    properties: Record<string, unknown>;
}
export interface AnalyticsEvent {
    data: SpadeEvent;
    transformProperties(...args: unknown[]): SpadeEvent['properties'];
}
export interface BroadcastEndpoint {
    protocol: string;
    hostname: string;
    port: string;
}
export interface ErrorEvent {
    value: number;
    code: number;
    description: string;
    isFatal: boolean;
    broadcastState: string;
    eventCount: number;
}
